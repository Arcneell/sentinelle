# Configuration locale (IP, identifiants) — jamais publiée
config.yaml
*.env
.env

# Secrets / certificats
*.cer
*.pfx
*.p12
*.key

# Référence externe (autres projets) et archives / imports
_ref/
_import/
*.zip

# Captures QR / images d'import (identifiants DVR)
new_qr_*.png
new_qr_*.jpg

# libmpv locale (dev Windows)
lib/

# Builds et livrables
build/
dist/
livraison/
*.spec

# Données du serveur (config centrale, jetons) — jamais publiées
deploy/data/

# Doc interne (non publiée)
CADRAGE.md

# Python
__pycache__/
*.pyc
.venv/
venv/
_diag/
# Added by code-review-graph
.code-review-graph/

# Configs outils IA locaux (chemins machine, jamais publiés)
.mcp.json
.claude/
.clinerules/
.codebuddy/
.cursor/
.gemini/
.opencode/
.qoder/
.vscode/
.windsurf/
opencode.jsonc
AGENTS.md
.github/copilot-instructions.md

# Diffs / patchs de travail locaux
*.diff
*.patch
